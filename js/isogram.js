function isIsogram(str){
   // create variable to store letters of given word
  // turn letters into lowercase so it works for all words
  let chars = str.toLowerCase();
  // split word into its letters
  chars = chars.split("");
}

console.log(isIsogram("mOose"));