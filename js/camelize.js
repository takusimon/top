// FUNCTION camelize (str)
    //Separate words & Store in an array
    //Capitalize every word except the first one
        //For each word starting at second index
            //Replace the first letter with its capitalized form
    //Join the words in the array into one string
    //Return the camalized string
//END FUNCTION
